# Seema-Ihbesheh-LibraryDB1
Set up the database connection and create database models (let Sequelize create your tables then). Create the following requests handlers: to book and publisher and comment
